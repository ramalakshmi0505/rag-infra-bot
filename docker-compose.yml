version: "3.8"

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: infra-rag-db
    environment:
      POSTGRES_USER: raguser
      POSTGRES_PASSWORD: ragpassword
      POSTGRES_DB: infra_rag
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U raguser -d infra_rag"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    container_name: infra-rag-api
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://raguser:ragpassword@postgres:5432/infra_rag
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
    ports:
      - "8000:8000"
    command: uvicorn src.app:app --host 0.0.0.0 --port 8000 --reload

volumes:
  pgdata:
